# =============================================================================
# Waterball LMS Frontend - Environment Variables
# =============================================================================
#
# 複製此檔案為：
#   - .env.local（本地開發，不走 Docker）
#   - .env.development（Docker 開發環境）
#   - .env.production（Docker 生產環境）
#
# 注意：所有 NEXT_PUBLIC_* 變數會暴露在瀏覽器端，不要放敏感資訊！
#
# =============================================================================

# =============================================================================
# 環境設定
# =============================================================================

# 環境模式: development | production | test
NEXT_PUBLIC_ENV=development

# 應用程式名稱
NEXT_PUBLIC_APP_NAME=Waterball LMS

# =============================================================================
# API 配置（後端 Spring Boot 服務）
# =============================================================================

# --- 開發環境（Docker Compose） ---
# 容器內部通訊使用 service name
NEXT_PUBLIC_API_URL=http://backend:8080
NEXT_PUBLIC_API_BASE_URL=http://backend:8080/api

# --- 開發環境（本地不使用 Docker） ---
# 直接連接 localhost 的後端
# NEXT_PUBLIC_API_URL=http://localhost:8080
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# --- 生產環境 ---
# 使用實際的域名或 IP
# NEXT_PUBLIC_API_URL=https://api.waterballsa.tw
# NEXT_PUBLIC_API_BASE_URL=https://api.waterballsa.tw/api

# 是否使用真實 API（false = 使用 Mock 資料）
# R1: false, R2: true
NEXT_PUBLIC_USE_REAL_API=false

# =============================================================================
# 前端應用程式 URL
# =============================================================================

# --- 開發環境 ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- 生產環境 ---
# NEXT_PUBLIC_APP_URL=https://world.waterballsa.tw

# =============================================================================
# LINE Login 配置（R2 功能）
# =============================================================================

# LINE Login Channel ID（公開，可在瀏覽器端使用）
NEXT_PUBLIC_LINE_CLIENT_ID=your_line_channel_id_here

# LINE Login Channel Secret（私密，僅後端使用，此處僅作記錄）
# 實際應在後端的 .env 中配置
# LINE_CLIENT_SECRET=your_line_channel_secret_here

# LINE Login 回調 URL
# 開發環境
NEXT_PUBLIC_LINE_REDIRECT_URI=http://localhost:3000/api/auth/line/callback

# 生產環境
# NEXT_PUBLIC_LINE_REDIRECT_URI=https://world.waterballsa.tw/api/auth/line/callback

# =============================================================================
# 功能開關（Feature Flags）
# =============================================================================

# 啟用分析工具（Google Analytics, Mixpanel 等）
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# 啟用錯誤回報（Sentry, Bugsnag 等）
NEXT_PUBLIC_ENABLE_ERROR_REPORTING=false

# 啟用除錯模式（顯示更多 console.log）
NEXT_PUBLIC_DEBUG=false

# =============================================================================
# 開發環境專用設定
# =============================================================================

# Mock API 延遲時間（毫秒），模擬網路延遲
NEXT_PUBLIC_MOCK_DELAY=500

# 是否顯示開發者工具
NEXT_PUBLIC_SHOW_DEV_TOOLS=false

# =============================================================================
# 外部服務 URL（R3+ 功能）
# =============================================================================

# 圖片上傳服務
NEXT_PUBLIC_UPLOAD_URL=

# WebSocket URL（即時通訊、通知推送）
NEXT_PUBLIC_WS_URL=

# CDN URL（靜態資源加速）
NEXT_PUBLIC_CDN_URL=https://cdn.waterballsa.tw

# =============================================================================
# Next.js 配置
# =============================================================================

# 是否在 standalone 模式下運行（用於 Docker 部署優化）
# 此變數在 next.config.js 中使用，不需要 NEXT_PUBLIC_ 前綴
OUTPUT_STANDALONE=true

# =============================================================================
# CORS 相關設定（供後端參考）
# =============================================================================

# 此部分變數應配置在後端的 .env 中
# 前端無需配置，僅作為文檔參考

# 開發環境允許的 CORS 來源
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000

# 生產環境允許的 CORS 來源
# CORS_ALLOWED_ORIGINS=https://world.waterballsa.tw

# =============================================================================
# 效能優化
# =============================================================================

# 圖片優化品質（1-100）
NEXT_PUBLIC_IMAGE_QUALITY=90

# 是否啟用圖片懶加載
NEXT_PUBLIC_ENABLE_LAZY_LOADING=true

# =============================================================================
# 監控與日誌
# =============================================================================

# Sentry DSN（錯誤追蹤）
# NEXT_PUBLIC_SENTRY_DSN=

# Google Analytics Tracking ID
# NEXT_PUBLIC_GA_TRACKING_ID=

# =============================================================================
# 備註
# =============================================================================
#
# 1. Docker Compose 開發環境：
#    - 前端使用 http://backend:8080 連接後端（容器內部網路）
#    - 瀏覽器透過 http://localhost:3000 訪問前端
#    - 瀏覽器透過 http://localhost:8080 訪問後端 API（用於測試）
#
# 2. 本地開發（不使用 Docker）：
#    - 前端使用 http://localhost:8080 連接後端
#    - 後端需要額外配置 CORS 允許 http://localhost:3000
#
# 3. 生產環境：
#    - 使用實際域名
#    - 啟用 HTTPS
#    - 配置反向代理（Nginx）
#    - 啟用快取和壓縮
#
# 4. CORS 配置（Spring Boot backend）：
#    - 開發環境：允許 http://localhost:3000 和 http://frontend:3000
#    - 生產環境：僅允許實際域名
#    - 確保後端配置了正確的 CORS headers
#
# =============================================================================
