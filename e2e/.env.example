# E2E Test Environment Configuration

# Test environment (local, ci, staging, prod)
TEST_ENV=local

# Frontend URL (uses dev environment ports)
BASE_URL=http://localhost:3000

# Backend API URL (uses dev environment ports)
API_URL=http://localhost:8080

# Test database configuration (uses dev environment settings)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=waterball_lms
DB_USER=postgres
DB_PASSWORD=postgres

# Test user credentials
FREE_USER_EMAIL=free-user@test.com
FREE_USER_PASSWORD=Test123456!
PAID_USER_EMAIL=paid-user@test.com
PAID_USER_PASSWORD=Test123456!

# Playwright configuration
HEADED=false
SLOW_MO=0
TIMEOUT=30000

# CI/CD configuration
CI=false
